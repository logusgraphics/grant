# Grant API base URL (required for authorization checks)
GRANT_API_URL=https://api.grant.example.com

# Optional: server port (default 3000)
# PORT=3000

# Optional: default token for local testing (otherwise use Authorization header)
# GRANT_TOKEN=your-jwt-from-api-key-exchange
