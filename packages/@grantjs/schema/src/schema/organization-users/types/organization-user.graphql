type OrganizationUser implements Auditable {
  id: ID!
  organizationId: ID!
  userId: ID!
  roleId: ID!
  organization: Organization
  user: User
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
