extend type Query {
  """
  Read the current state of a project CDM sync job. Use this to poll
  the lifecycle of a job started via `startProjectSync`.
  """
  projectSyncJob(id: ID!, scope: Scope!, jobId: ID!): ProjectSyncJob!
}
