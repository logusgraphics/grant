type ProjectUser implements Auditable {
  id: ID!
  projectId: ID!
  userId: ID!
  metadata: JSON!
  displayName: String
  pictureUrl: String
  project(organizationId: ID!): Project
  user: User
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
