enum AuthorizationReason {
  INVALID_SCOPE
  NOT_AUTHENTICATED
  INVALID_AUTHENTICATION_STATE
  NO_MATCHING_PERMISSION_FOUND
  PERMISSION_GRANTED_NO_CONDITION
  PERMISSION_GRANTED_CONDITION_MET
  PERMISSION_FOUND_CONDITION_NOT_MET
  SCOPE_NOT_GRANTED
}
