type Resource implements Auditable {
  id: ID!
  name: String!
  slug: String!
  description: String
  actions: [String!]!
  isActive: Boolean!
  metadata: JSON!
  tags: [Tag!]!
  permissions: [Permission!]
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
