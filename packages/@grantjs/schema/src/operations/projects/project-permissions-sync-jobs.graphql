query ProjectPermissionsSyncJobs(
  $id: ID!
  $scope: Scope!
  $page: Int
  $limit: Int
  $search: String
  $sort: ProjectPermissionsSyncJobSortInput
  $status: ProjectPermissionsSyncJobStatus
) {
  projectPermissionsSyncJobs(
    id: $id
    scope: $scope
    page: $page
    limit: $limit
    search: $search
    sort: $sort
    status: $status
  ) {
    jobs {
      id
      projectId
      status
      cdmVersion
      importId
      warnings
      errorMessage
      enqueuedAt
      startedAt
      completedAt
      cancelledAt
      hasSnapshot
      snapshotTakenAt
      snapshotSizeBytes
    }
    totalCount
    hasNextPage
  }
}
