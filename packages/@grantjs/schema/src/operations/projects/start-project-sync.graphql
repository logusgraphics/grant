mutation StartProjectSync($id: ID!, $scope: Scope!, $input: SyncProjectInput!) {
  startProjectSync(id: $id, scope: $scope, input: $input) {
    id
    projectId
    status
    cdmVersion
    importId
    warnings
    errorMessage
    enqueuedAt
    startedAt
    completedAt
    cancelledAt
  }
}
