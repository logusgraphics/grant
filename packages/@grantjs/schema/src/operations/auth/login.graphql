mutation Login($input: LoginInput!) {
  login(input: $input) {
    accessToken
    refreshToken
    mfaVerified
    requiresMfaStepUp
    requiresEmailVerification
    verificationExpiry
    email
    accounts {
      id
      type
      owner {
        id
        name
        pictureUrl
        updatedAt
      }
    }
  }
}
