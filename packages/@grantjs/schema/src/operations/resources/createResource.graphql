mutation CreateResource($input: CreateResourceInput!) {
  createResource(input: $input) {
    id
    name
    slug
    description
    actions
    isActive
    createdAt
    updatedAt
    permissions {
      id
      name
      action
      resourceId
    }
  }
}
