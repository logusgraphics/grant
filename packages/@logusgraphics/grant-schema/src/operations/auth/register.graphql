mutation Register($input: RegisterInput!) {
  register(input: $input) {
    accessToken
    refreshToken
    requiresEmailVerification
    verificationExpiry
    email
    account {
      id
      name
      slug
      type
    }
  }
}
