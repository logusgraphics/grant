input GetUserSessionsInput {
  page: Int
  limit: Int
  search: String
  sort: UserSessionSortInput
  ids: [ID!]
  userId: ID
  audience: String
  expiresAtMin: Date
  userAgent: String
  ipAddress: String
  requestedFields: [String!]
}
