type Role implements Auditable {
  id: ID!
  name: String!
  description: String
  groups(scope: Scope!): [Group!]
  tags(scope: Scope!): [Tag!]
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
