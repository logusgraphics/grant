type PermissionTag implements Auditable {
  id: ID!
  permissionId: ID!
  tagId: ID!
  permission(scope: Scope!): Permission
  tag(scope: Scope!): Tag
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
