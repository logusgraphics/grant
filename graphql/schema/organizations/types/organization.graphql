type Organization implements Auditable {
  id: ID!
  name: String!
  slug: String!
  projects: [Project!]
  roles: [Role!]
  groups: [Group!]
  permissions: [Permission!]
  users: [User!]
  tags: [Tag!]
  createdAt: String!
  updatedAt: String!
}
