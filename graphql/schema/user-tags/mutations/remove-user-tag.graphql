extend type Mutation {
  """
  Removes a user-tag relationship.
  """
  removeUserTag(
    """
    Input data for removing the user-tag relationship.
    """
    input: RemoveUserTagInput!
  ): Boolean!
}
