type Group implements Auditable {
  id: ID!
  name: String!
  description: String
  permissions(scope: Scope!): [Permission!]
  tags(scope: Scope!): [Tag!]
  createdAt: String!
  updatedAt: String!
}
