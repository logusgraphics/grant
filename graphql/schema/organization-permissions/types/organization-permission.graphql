type OrganizationPermission implements Auditable {
  id: ID!
  organizationId: ID!
  permissionId: ID!
  organization: Organization
  permission: Permission
  createdAt: Date!
  updatedAt: Date!
  deletedAt: Date
}
