interface Auditable {
  id: ID!
  createdAt: String!
  updatedAt: String!
}
interface Creatable {
  createdAt: String!
  updatedAt: String!
}
interface PaginatedResults {
  totalCount: Int!
  hasNextPage: Boolean!
}
enum Tenant {
  ORGANIZATION
  PROJECT
}
input Scope {
  tenant: Tenant!
  id: ID!
}
