type GroupPermission implements Auditable {
  id: ID!
  groupId: ID!
  permissionId: ID!
  group(scope: Scope!): Group
  permission(scope: Scope!): Permission
  createdAt: String!
  updatedAt: String!
}
