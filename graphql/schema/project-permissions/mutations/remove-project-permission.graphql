extend type Mutation {
  """
  Removes a project-permission relationship.
  """
  removeProjectPermission(
    """
    Input data for removing the project-permission relationship.
    """
    input: RemoveProjectPermissionInput!
  ): Boolean!
}
