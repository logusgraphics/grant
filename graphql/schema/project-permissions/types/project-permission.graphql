type ProjectPermission implements Auditable {
  id: ID!
  projectId: ID!
  permissionId: ID!
  project(organizationId: ID!): Project
  permission: Permission
  createdAt: String!
  updatedAt: String!
}
