type Project implements Auditable {
  id: ID!
  name: String!
  slug: String!
  description: String
  roles: [Role!]
  groups: [Group!]
  permissions: [Permission!]
  users: [User!]
  tags: [Tag!]
  createdAt: String!
  updatedAt: String!
}
