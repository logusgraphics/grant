export const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key';

// Note: In production, always use an environment variable for JWT_SECRET
// and never commit the actual secret to version control
